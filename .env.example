# Copy to .env and add your keys. Do not commit .env.

# Required: OpenAI API key (for extraction, synthesis, self-critique)
OPENAI_API_KEY=

# Optional: News API (more articles; get key from https://newsapi.org)
# NEWS_API_KEY=

# Optional: limit docs per run (leave empty for no limit)
# MAX_DOCS_PER_RUN=50

# Optional: process tracking
# TRACK_STATUS_FILE=1     # write data/run_status.json (default 1)
# TRACK_PROGRESS=1        # log extract progress every 5 docs
# LOG_LEVEL=INFO          # DEBUG, INFO, WARNING, ERROR
